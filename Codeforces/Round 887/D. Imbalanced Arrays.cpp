#include<bits/std++.h>
#define ll long long
#define dl double
#define ar array
const int maxn=1e9;
using namespace std;
void solve()
{
    int n;
    cin>>n;
    vector<int>ar(n);
    for(int i =0;i<n;i++)
    {
        cin>>ar[i];
    }
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin>>t;
    while(t--)
    {
        solve();
    }
    return 0;
}
